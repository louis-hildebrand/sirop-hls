const n: u32 = 10
const m: u32 = (n -% 1:u32) / 2:u32
const coeffs: Vec[u16, m] = vbuild(m) { (_: u32) => 5:u16 }

accelerator add5 = (s: Stm[Vec[u16, m], n]) =>
    s.StmMap((v) =>
        v.VecZip(coeffs)
            .VecMap((x) => x.0 + x.1)
    )
