(I : Stm[i8, 16:u5]) =>
  sbuild(
    16:u5;
    acc +% data(s);
    t == 2:u2;
    (acc : i8) = (
      0:i8,
      if (t == 2:u2) then {
        0:i8
      } else if (first_step) then {
        data(s)
      } else {
        acc +% data(s)
      }
    );
    (first_step : bool) = (
      true,
      t == 2:u2
    );
    (s : Stm[i8, 3:u2]) = (
      sbuild(
        48:u7;
        data(I0) *% data(I1);
        true;
        (I0 : Stm[i8, -1:i1]) = (
          sbuild(
            48:u8;
            if (is_first_step) then { data(x_stm)[0:u32] } else { x_data[i] };
            true;
            (i : u32) = (
              0:u32,
              if (
                !is_first_step && in_ctr == 1:u32 && out_ctr == 2:u32
                  || in_ctr == 0:u32 && out_ctr == 2:u32 && is_first_step
              ) then {
                0:u32
              } else {
                1:u32 + (if (is_first_step) then { 0:u32 } else { i })
              }
            );
            (in_ctr : u32) = (
              0:u32,
              if (
                !is_first_step && in_ctr == 1:u32 && out_ctr == 2:u32
                  || in_ctr == 0:u32 && out_ctr == 2:u32 && is_first_step
              ) then {
                0:u32
              } else if (is_first_step) then {
                1:u32 + in_ctr
              } else {
                in_ctr
              }
            );
            (is_first_step : bool) = (
              true,
              !is_first_step && in_ctr == 1:u32 && out_ctr == 2:u32
                || in_ctr == 0:u32 && out_ctr == 2:u32 && is_first_step
            );
            (out_ctr : u32) = (
              0:u32,
              if (
                !is_first_step && in_ctr == 1:u32 && out_ctr == 2:u32
                  || in_ctr == 0:u32 && out_ctr == 2:u32 && is_first_step
              ) then {
                0:u32
              } else {
                1:u32 + out_ctr
              }
            );
            (x_data : Vec[i8, 3:u3]) = (
              vbuild(3:u3, (i : u32) => 0:i8),
              if (
                !is_first_step && in_ctr == 1:u32 && out_ctr == 2:u32
                  || in_ctr == 0:u32 && out_ctr == 2:u32 && is_first_step
              ) then {
                vbuild(3:u3, (i : u32) => 0:i8)
              } else if (is_first_step) then {
                data(x_stm)
              } else {
                x_data
              }
            );
            (x_stm : Stm[Vec[i8, 3:u3], -1:i1]) = (
              sbuild(
                16:u5;
                vbuild(
                  3:u3,
                  (i : u32) =>
                    if (i < 2:u32) then {
                      if (i == 0:u32) then {
                        buf_1[0:u0]
                      } else {
                        buf_446[0:u0]
                      }
                    } else {
                      data(I1)
                    }
                );
                true;
                (I1 : Stm[i8, -1:i1]) = (
                  I,
                  true
                );
                (buf_1 : Vec[i8, 1:u1]) = (
                  vbuild(1:u1, (i : u32) => 0:i8),
                  vbuild(1:u2, (i : u32) => buf_446[0:u0])
                );
                (buf_446 : Vec[i8, 1:u1]) = (
                  vbuild(1:u1, (i : u32) => 0:i8),
                  vbuild(1:u2, (i : u32) => data(I1))
                );
              ),
              is_first_step
            );
          ),
          true
        );
        (I1 : Stm[i8, -1:i1]) = (
          sbuild(
            48:u7;
            [-1:i8, 0:i8, 1:i8][i];
            true;
            (i : u2) = (
              0:u2,
              if (i == 2:u2) then { 0:u2 } else { 1:u2 + i }
            );
          ),
          true
        );
      ),
      true
    );
    (t : u2) = (
      0:u2,
      if (t == 2:u2) then { 0:u2 } else { 1:u2 + t }
    );
  )