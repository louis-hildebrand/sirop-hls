const KERNEL: Vec[i8, 3] = [-1:i8, 0:i8, 1:i8]v

accelerator
top = (I: Stm[i8, 16]) =>
    I
    .StmSlide(3)
    .StmMap( (window: Vec[i8, 3]) =>
        VecZip(window, KERNEL)
        .VecMap( (x) => x.0 *% x.1 )
        .VecReduce( (x) => x.0 +% x.1 )[0]
    )
